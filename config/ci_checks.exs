import Config

# A custom environment used to avoid compilation of credo, dialyxir and excoverallsin
# in other environments.  It's recommended to use this environment only by regular CI script

import_config "test.exs"
