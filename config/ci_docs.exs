import Config

# A custom environment used to avoid compilation of ex_doc in other environments
# It's recommended to use this environment only by CI script
# which is triggered after merge to master/main branch
